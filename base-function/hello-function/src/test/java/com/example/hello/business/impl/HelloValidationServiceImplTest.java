package com.example.hello.business.impl;

import static org.junit.jupiter.api.Assertions.assertTrue;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
public class HelloValidationServiceImplTest {

    // @InjectMocks
    // HelloValidationServiceImpl target;
    // @Mock
    // MessageSource messageSource;

    @Test
    void testExecute() {
        // doReturn("aaa").when(messageSource).getMessage(Mockito.any(), Mockito.any(),
        // Mockito.any());
        assertTrue(true);
    }
}
